import {} from '../claimsProcessor';
import { testPolicies } from '../testData';

describe('ClaimsProcessor', () => {
  // initialize variables
  beforeEach('', () => {});

  // add test blocks
  test('', () => {
    // expect().toBe.()
  });
});
